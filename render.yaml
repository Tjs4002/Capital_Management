services:
  - type: web
    name: capital-management
    env: node
    plan: free
    buildCommand: npm run init-db
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
